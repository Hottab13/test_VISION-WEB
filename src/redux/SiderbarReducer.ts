let initionalState = {} as {} 
export type InitionalStateType = typeof initionalState

const sidebarReducer=(state=initionalState,action:any):InitionalStateType=> {
 return state
    }
    
    export default sidebarReducer